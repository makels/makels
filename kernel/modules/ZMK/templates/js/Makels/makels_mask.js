/*
	Makels mask element
*/

var MakelsMask = function(config) {

	this.masked = (config && config.masked) ? config.masked : "body";

	// Initialization
	this.init = function() {

	}

	// Mask show
	this.show = function() {

	}

	// Mask hide
	this.hide = function() {

	}

}